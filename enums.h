#ifndef ENUMS_H
#define ENUMS_H

enum MESHES {
    CUBE,
    RECTANGLE,
};

enum COMPONENT {
    TRANSFORM,
    MOVEMENT,
};

enum class SHADERS {
    TEXTURE,
    COLOR,
    PHONG,
    LIGHT_SOURCE
};

#endif // ENUMS_H
